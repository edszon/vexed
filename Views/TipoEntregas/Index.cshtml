@model IEnumerable<vexed.Models.TipoEntrega>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoDeEntrega)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoDeEntrega)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdTipoEntrega }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdTipoEntrega }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdTipoEntrega })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
