@model IEnumerable<vexed.Models.CupomDesconto>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PercentualDesconto)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PercentualDesconto)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdCupomDesconto }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdCupomDesconto }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdCupomDesconto })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
